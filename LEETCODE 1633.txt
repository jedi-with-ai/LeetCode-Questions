LEETCODE 1633 
# Write your MySQL query statement below
SELECT contest_id, ROUND(count(user_id)/(SELECT COUNT(*) FROM Users)*100,2) AS percentage FROM Register
GROUP BY contest_id
ORDER BY percentage DESC, contest_id ASC;

-- In some question you dont need to join even if there are two or more tables. There might be a way to subquery and solve it without joining. 